# Server-only Vercel project environment variables. Do not prefix these with VITE_.
# BACKEND_API_URL points to the deployed Express API origin, without a trailing slash.
BACKEND_API_URL=https://your-backend.example.com
# VERCEL_BYPASS_SECRET is the deployment protection bypass secret for the backend.
VERCEL_BYPASS_SECRET=replace-me
